services:
  - type: web
    name: s50-forward-test
    runtime: python
    region: singapore
    plan: free
    branch: master
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn render_app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SUPABASE_URL
        value: https://roxiyshdxpymmcnsiqpz.supabase.co
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false

  - type: cron
    name: s50-check-all
    runtime: python
    region: singapore
    plan: free
    branch: master
    schedule: "*/15 * * * *"
    buildCommand: pip install -r requirements.txt
    startCommand: python -c "import httpx; r=httpx.get('https://s50-forward-test.onrender.com/api/cron/check-all', timeout=120); print(r.text)"
